export const AxisOffset = {
  x: 10,
  y: 10,
}
export const ScrollBarWidth = 10;

function calc(ri:number, ci:number, grid:ICell[][]) {
  return 0
}
// function obser(range: IRange, grid:ICell[][], set, renderRerender: () => void) {
//   const {ri, ci, eri, eci} = range;
// }
